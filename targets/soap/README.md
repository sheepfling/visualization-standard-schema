# SOAP Target

This directory is reserved for compile rules that translate the core VSS
message model into SOAP-compatible message structures.

Planned scope:

- envelope to SOAP body mapping
- field naming and namespace rules
- schema narrowing for strongly typed consumers
- example generated SOAP payloads
